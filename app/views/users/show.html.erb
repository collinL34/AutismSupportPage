<!--Card-->
<div class='container profile-container'>
  <div class="card testimonial-card">

      <!--Bacground color-->
      <div class="card-up default-color-dark danger-color">
      <p>Welcome <%=@user.first_name%> to your Profile Page </p>
      </div>

      <!--Avatar-->
      <% if @user.profile_img %>
        <div class="avatar">
        <img src="<%=@user.profile_img%>" class="rounded-circle img-responsive profile">

      <%else%>
        <%= form_for @user do |f| %>
          <%= f.text_field :profile_img, placeholder: 'Image Link' %>
        <% end %>
      <% end %>
    
        </div>

        <div class="divider-new">
          <h2 class="h2-responsive wow fadeInDown">Friends</h2>
        </div>

      <%= render :partial => 'friends_carousel' %>

      <div class="divider-new">
        <h2 class="h2-responsive wow fadeInDown">Articles</h2>
      </div>

      <%= render :partial => 'articles_carousel' %>
  </div>
  <!--/.Card-->
</div>