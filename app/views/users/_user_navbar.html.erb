<!--Navbar-->
    <nav class="navbar navbar-dark navbar-fixed-top scrolling-navbar">
        <div class="container">
            <!--Links-->
            <a class="navbar-brand purple-text" href="/">Autism Talks</a>
            <ul class="nav navbar-nav">
                <li>
                <% if session[:user_id] %>
                    <li><%= link_to 'Logout', :logout, class: 'green-text'%></li>
                    <li><%= link_to 'Profile', @user, class: 'yellow-text'%></li>
                <% else %>
                    <li><%= link_to 'Log In Now!', :login, class: 'blue-text' %></li>
                    <li><%= link_to 'Sign Up Now!', :new_user, class: 'purple-text' %></li>
                <% end %>

                </li>
            </ul>
            
            <%= form_tag(root_path, :method => "get", id: "search-form", class:"form-inline float-lg-right") do %>
                    <%= text_field_tag :search, params[:search], placeholder: "Search Categories", class:"form-control green-text search" %>
                    <%= submit_tag "Search", :name => nil, class: 'btn btn-danger btn-sm' %>
            <% end %>
        </div>
    </nav>
    <!--/.Navbar-->